'use strict';

module.exports = {
    html: {
        options: {
            port: 9000,
            base: 'app',
            livereload: true,
            hostname:'localhost',
            // hostname:'0.0.0.0' // network availability
        }
    }
};